name: Build

on: 
  workflow_dispatch:
  push:
    branches:
      - master

env:
  DOTNET_VERSION: "8.x"

jobs:
  build: 
    runs-on: ubuntu-latest

    steps: 
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with: 
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Clean Solution
        run: dotnet clean ./TraffiLearn.sln

      - name: Restore
        run: dotnet restore ./TraffiLearn.sln

      - name: Build
        run: dotnet build ./TraffiLearn.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test ./TraffiLearn.sln --configuration Release --no-restore --no-build

      - name: Publish Test Results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: test-results
          path: '**/TestResults/*.trx'

      - name: Notify on Failure
        if: failure()
        run: echo "Build failed."
